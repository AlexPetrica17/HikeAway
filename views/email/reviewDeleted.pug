extends baseEmail

block content
  h1 Recenzia ta a fost ștearsă
  p Salut #{firstName},

  - var tourName = (review && review.tour && review.tour.name) ? review.tour.name : 'necunoscut'

  p
    | Recenzia ta pentru turul #{tourName} a fost ștearsă de un administrator.

  if reason
    p Motivul ștergerii:
    blockquote #{reason}
  else
    p Administratorul nu a specificat un motiv.

  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tr
      td
        a(href=url, target='_blank') Vezi recenziile tale
